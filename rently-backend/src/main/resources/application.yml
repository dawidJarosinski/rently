spring:
  application:
    name: rently-backend
  datasource:
    url: jdbc:postgresql://localhost:5432/rently-db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update